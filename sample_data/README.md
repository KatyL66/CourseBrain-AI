# Sample data

Fictional course materials for trying the backend without a Brightspace login.

`intro-numerical-methods/ingest_batch.json` matches `POST /api/v1/ingest/batch`.

It is not a real course. Do not treat the text as academic source material.
